#pragma once
#include <filesystem>
#include <string>

// Appended after the shared source Float3, ZunTimer and math definitions.
std::string enemy_motion_reference(const std::filesystem::path &repo);
